#pragma once

#include <iostream>		//Standard libraries
#include <string>
#include <fstream>
#include <iomanip>

using namespace std;

void inputData(const char*);	//Prototypes of functions
void outputFile(const char*);
void blackList(const char*, const char*);

